import React, {useEffect} from 'react';

const Content = (props) => {
    useEffect(() => {

    }, []);

    return <h1>TTTThe product to search in FB: {props.product}</h1>;
};

export default Content;